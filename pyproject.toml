[project]
name = "auto-quant-stock"
version = "0.1.0"
description = "LLM-native autonomous quant research loop for US stocks"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "backtesting>=0.3.3",
    "yfinance>=0.2.31",
    "pandas>=2.2",
    "numpy>=1.26",
    "pyarrow>=14.0",
]

[tool.uv]
package = false
